{
  "name": "my-ntu-pp",
  "private": true,
  "workspaces": [
    "Frontend",
    "Backend"
  ],
  "scripts": {
    "dev": "bun --cwd Backend run dev & bun --cwd Frontend run dev",
    "dev:fe": "bun --cwd Frontend run dev",
    "dev:be": "bun --cwd Backend run dev",
    "install:all": "bun install",
    "lint": "bun run --filter '*' lint",
    "typecheck": "bun run --filter '*' typecheck"
  }
}
